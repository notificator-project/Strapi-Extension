import activity from './activity';
import delivery from './delivery';
import settings from './settings';

/** Service registry consumed by Strapi's plugin loader. */
export default {
  activity,
  delivery,
  settings,
};
